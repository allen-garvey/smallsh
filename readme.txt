# smallsh

A simple shell interpreter written in C.

## Dependencies

* gcc 4.4.7 or higher
* A UNIX/Linux compatible operating system

## Getting Started

* Upload this directory to flip
* `cd` into the project directory and type `make` (without the backticks)
* Type `./smallsh` (again without the backticks) to start smallsh

